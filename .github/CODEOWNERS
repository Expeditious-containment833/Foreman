# Every path is owned by the author, so every pull request automatically
# requests his review. This does not grant anyone else write access; it only
# makes sure nothing is reviewed by accident.
*  @Turki-Sh
